package com.simple.pulsejob.admin.common.model.enums;

/**
 * 运行模式枚举
 * AI Generated
 */
public enum GlueTypeEnum {
    
    /**
     * BEAN模式 - 基于Spring Bean执行
     */
    BEAN,
    
    /**
     * GLUE模式(Groovy) - 在线编辑Groovy脚本
     */
    GLUE_GROOVY,
    
    /**
     * GLUE模式(Shell) - 在线编辑Shell脚本
     */
    GLUE_SHELL,
    
    /**
     * GLUE模式(Python) - 在线编辑Python脚本
     */
    GLUE_PYTHON,
    
    /**
     * GLUE模式(PowerShell) - 在线编辑PowerShell脚本
     */
    GLUE_POWERSHELL
}
